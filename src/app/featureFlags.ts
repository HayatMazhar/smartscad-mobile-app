/**
 * Central toggles for features that should be easy to turn back on.
 * Set `AI_ASSISTANT_ENABLED` to true when the assistant is ready to ship again.
 */
export const AI_ASSISTANT_ENABLED = false;
